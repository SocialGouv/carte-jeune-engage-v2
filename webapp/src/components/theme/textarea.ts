import { defineStyleConfig } from "@chakra-ui/react";

export const textareaTheme = defineStyleConfig({
	baseStyle: {
		minH: '150px',
		paddingTop: '0 !important',
		borderTop: '30px solid transparent !important',
	}
})