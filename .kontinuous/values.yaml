app:
  ~chart: app
  ~needs: [build-app]
  imagePackage: app
  probesPath: /api/healthz

  env:
    - name: DATABASE_URL
      value: "$(DATABASE_URL)"

pg:
  ~chart: pg

jobs:
  runs:
    build-app:
      use: build
      with:
        imagePackage: app
        context: ./webapp
